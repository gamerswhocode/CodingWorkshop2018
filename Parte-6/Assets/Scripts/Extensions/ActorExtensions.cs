using UnityEngine;

public static class ActorExtensions {


    /// <summary>
    /// Returns true if object in questions is behind target relative to object position.
    /// </summary>
    /// <param name="objectPosition"></param>
    /// <param name="targetPosition"></param>
    /// <returns></returns>
    public static bool IsBehind(this Vector3 objectPosition, Vector3 targetPosition)
    {
        return false;
    }
}
